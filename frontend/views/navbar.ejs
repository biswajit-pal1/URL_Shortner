<link rel="stylesheet" href="style.css" />
<nav class="navbar navbar-expand-sm bg-white border-bottom sticky-top py-3">
  <div class="container-fluid middle">
    <!-- Brand (icon + text aligned) -->
    <a href="/" class="navbar-brand d-flex align-items-baseline text-dark mb-0">
      <i class="fa-solid fa-link me-2 fs-5"></i>
      <span class="fw-semibold fs-5">Urlink</span>
    </a>

    <!-- Toggler (visible on xs screens) -->
    <button
      class="navbar-toggler ms-auto"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarRight"
      aria-controls="navbarRight"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible Menu - Updated alignment classes -->
    <div class="collapse navbar-collapse flex-grow-0" id="navbarRight">
      <ul class="navbar-nav align-items-sm-center text-start mt-2 mt-sm-0">
        <% if (!currUser) { %>
          <li class="nav-item">
            <a class="nav-link fw-bold" href="/signup">Sign up</a>
          </li>
          <li class="nav-item">
            <a class="nav-link fw-bold" href="/login">Log in</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link fw-bold" href="/profile">My Links</a>
          </li>
          <li class="nav-item">
            <a class="nav-link fw-bold" href="/logout">Log out</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>